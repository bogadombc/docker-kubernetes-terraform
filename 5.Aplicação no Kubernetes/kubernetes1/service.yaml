apiVersion: v1
kind: Service
metadata:
  name: app-k8s-service
  namespace: curso-k8s
spec:
  type: NodePort
  selector:
    app: app-k8s
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
